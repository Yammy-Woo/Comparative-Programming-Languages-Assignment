class AlexandrianInteger {
    public static getNext(int n) {
        
    }
}

public class euler_221_alexandrian_integers {
    static int ord = 150000;    // Set the ordinal number of Alexandrian number to 150000 by default as required in the original question
    public static void main(String[] args)
    {
        if (args.length > 0)    // Take input from command line if given
        {
            limit = Integer.parseInt(args[0]); 
        }

        System.out.println();
    }
}
